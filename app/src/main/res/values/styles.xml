<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="alertDialogTheme">@style/AlertDialogTheme</item>
        <!--定义Actionbar上Menu的字体样式-->
        <item name="actionMenuTextAppearance">@style/NoCapsBarMenu</item>
        <item name="actionMenuTextColor">@color/white</item>
        <item name="homeAsUpIndicator">@drawable/icon_action_back</item>
    </style>

    <style name="WhiteActionBar">
        <item name="actionMenuTextAppearance">@style/NoCapsBarMenu</item>
        <item name="actionMenuTextColor">@color/colorAccent</item>
        <item name="titleTextColor">@color/black</item>
        <item name="subtitleTextColor">@color/black</item>
    </style>

    <style name="NoCapsBarMenu">
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="AlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="buttonBarNeutralButtonStyle">@style/AlertDialogBtnStyle</item>
        <item name="buttonBarNegativeButtonStyle">@style/AlertDialogBtnStyle</item>
        <item name="buttonBarPositiveButtonStyle">@style/AlertDialogBtnStyle</item>
    </style>

    <style name="AlertDialogBtnStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="titleTextColor">@color/white</item>
    </style>

    <style name="LaunchPageTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/bg_launch_gradient</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ButtonSolidStyle">
        <item name="android:height">42dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/button_solid_text_selector</item>
        <item name="android:background">@drawable/button_solid_background_selector</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="ButtonStrokeStyle">
        <item name="android:height">42dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/button_stroke_text_selector</item>
        <item name="android:background">@drawable/button_stroke_background_selector</item>
        <item name="android:gravity">center</item>
    </style>

    <!--通用对话框样式，兼容旧版本手机-->
    <style name="CommonDialogStyle" parent="Animation.AppCompat.Dialog">
        <!--设置动画，在这里使用让它继承系统的Animation.Dialog-->
        <!--是否设置标题-->
        <item name="android:windowNoTitle">true</item>
        <!--设置满屏-->
        <item name="android:windowFullscreen">true</item>
        <!--背景是否显示阴影-->
        <item name="android:backgroundDimEnabled">true</item>
        <!--设置窗口内容无浮层效果-->
        <item name="android:windowContentOverlay">@null</item>
        <!--是否半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--设置Dialog无windowFrame框-->
        <item name="android:windowFrame">@null</item>
        <!--设置背景颜色-->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
